{
  "scripts": {
    "bench": "bun bench/game.ts",
    "build": "bun build.js",
    "watch": "watchexec bun build.js",
    "check": "tsc --skipLibCheck --noEmit src/*.ts --target es2022",
    "test": "bun test"
  },
  "dependencies": {
    "esbuild": "0.24.0",
    "fast-check": "^3.22.0",
    "mitata": "^1.0.10",
    "term-canvas": "^0.0.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.11",
    "knip": "^5.33.3",
    "typescript": "^5.6.3"
  }
}
